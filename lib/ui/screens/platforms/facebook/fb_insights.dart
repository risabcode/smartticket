import 'package:flutter/material.dart';

class FBInsights extends StatelessWidget {
  const FBInsights({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Facebook Insights',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
