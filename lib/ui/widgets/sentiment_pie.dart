import 'package:flutter/material.dart';

class SentimentPie extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(height: 150, child: Center(child: Text('Pie chart placeholder')));
  }
}
